// const typingParagraph = document.querySelector('.typing-paragraph');
// const section = document.querySelector('.typing-section');
// const windowHeight = window.innerHeight;

// function animateParagraph() {
//     const sectionTop = section.getBoundingClientRect().top;
//     const sectionBottom = section.getBoundingClientRect().bottom;

//     // Check if the section is in view
//     if (sectionTop <= windowHeight / 2 && sectionBottom >= windowHeight / 2) {
//         typingParagraph.classList.add('typing');
//     } else {
//         typingParagraph.classList.remove('typing');
//     }
// }

// window.addEventListener('scroll', animateParagraph);
